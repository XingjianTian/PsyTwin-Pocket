# 演示求助事件与心宠难过表情

- [x] “演示求助”创建的本地事件在后续服务端状态同步后继续显示。*(已于 2026-07-27 增加独立持久状态，并在权威状态应用时重新合并，修复晚到响应覆盖事件的竞态问题)*
- [x] 创建演示求助事件后，请求实体心宠执行“难过”表情。*(已于 2026-07-27 完成 Sentinel 契约、Pocket 请求封装与安全白名单映射；同日修复异步回调位置导致的微信编译错误)*

## 行为约束

- 服务端事件仍以服务端响应为准；本地只保留明确标记为 `source: 'demo'` 的演示事件。
- 相同事件 ID 只显示一次。
- Pocket 只发送语义化的 `sad` 表情；Sentinel 将其固定映射到安全白名单中的 `emotion/sad1` 动作。
