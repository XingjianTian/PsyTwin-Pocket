# 演示求助事件与心宠难过表情

- [x] “演示求助”创建的本地事件在后续服务端状态同步后继续显示。*(已于 2026-07-27 增加独立持久状态，并在权威状态应用时重新合并，修复晚到响应覆盖事件的竞态问题)*
- [x] 创建演示求助事件后，请求实体心宠执行“难过”表情。*(已于 2026-07-27 完成 Sentinel 契约、Pocket 请求封装与安全白名单映射；同日修复异步回调位置导致的微信编译错误)*
- [x] 心宠同步服务器接收 Sentinel 温馨通知联动事件，持久化到共享心宠并广播给在线小程序。*(已于 2026-08-04 完成鉴权、幂等写入与状态广播)*
- [x] Pocket 将 Sentinel 低级别事件展示为“温馨通知”，使用柔和绿色并引导学生前往线下体验放松。*(已于 2026-08-05 完成服务端事件回归与页面文案接入)*
- [x] 现有求助事件接口返回全部未解决且未过期的服务端事件。*(已于 2026-08-04 完成，主页和独立事件页无需新增展示逻辑)*
- [x] Pocket 使用唯一 `petSyncUrl` 配置同时完成 HTTP 事件拉取和 WebSocket 实时状态接收。*(已于 2026-08-04 配置为局域网心宠服务器 `192.168.0.102:13002`)*
- [x] 演示期间 WebSocket 重连失败不弹出网络错误提示，继续使用自动重连与本地状态兜底。*(已于 2026-08-05 完成)*

## 行为约束

- 服务端事件仍以服务端响应为准；本地只保留明确标记为 `source: 'demo'` 的演示事件。
- 相同事件 ID 只显示一次。
- Pocket 只发送语义化的 `sad` 表情；Sentinel 将其固定映射到安全白名单中的 `emotion/sad1` 动作。
